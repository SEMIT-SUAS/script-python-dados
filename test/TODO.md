## Pendências
- [x] Alterar `numero_volume` para ficar como `NULL` (conforme amostra do banco).
- [x] Implementar verificação automática da `chave_lote`: o script deve ler o último valor atribuído no banco (ex: `L20`) e incrementar a partir dele, em vez de recomeçar do 1.
